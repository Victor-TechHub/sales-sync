const StepsTitle = ({ title }: { title: string }) => {
  return (
    <header className="py-5">
      <h3 className="text-lg font-medium">{title}</h3>
    </header>
  );
};

export default StepsTitle;
